# === REQUIRED (Vercel) ===
# Create a Postgres DB (Neon/Supabase/Railway). Then set this in Vercel → Project → Settings → Environment Variables.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"

# Demo mode allows org-picking without external auth in early demos.
DEMO="true"
